package serviceLayerTests;

public class TransactionHistoryRetrievalTests {

	// These tests should, similar to the data retrieval tests, retrieve information
	// From a separate database table that is tied to the same user
	
	// Retrieve Known Information Test
		// This test should successfully retrieve a known instance of data within the database
	
	// Fail to Retrieve Known Non-existent Information Test
		// Should properly handle data that does not exist within the table
	
	// Add to Known Table Test
		// Should successfully add valid data to a known instance within the table
	
	// Unsuccessful Data Input Test
		// Should fail to add wrong data to the table

}
